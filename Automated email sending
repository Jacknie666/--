# SMTP server address. In practice, you should replace 'smtp.example.com' 
# with the actual SMTP server address of your email service provider. 
# For example, it's'smtp.gmail.com' for Gmail and'smtp.qq.com' for QQ mail.
SMTP_SERVER = 'smtp.example.com'

# SMTP port number. Usually, 587 is used with STARTTLS encryption, 
# and 465 is used with SSL encryption. Here we keep 587 as a common example.
SMTP_PORT = 587

# Sender's email address. Replace 'your_email@example.com' 
# with your real and valid email address.
EMAIL_ADDRESS = 'your_email@example.com'

# Email password or authorization code. If you're using QQ mail, 
# you should use the authorization code instead of the regular login password. 
# Replace 'your_email_password' with the actual password or code.
EMAIL_PASSWORD = 'your_email_password'

# Recipient's email address. Replace'recipient@example.com' 
# with the real email address of the person you want to send the email to.
RECIPIENT_EMAIL ='recipient@example.com'
